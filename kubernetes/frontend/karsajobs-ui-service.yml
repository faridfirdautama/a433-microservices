apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui
  labels:
    app: karsa-frontend
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 8000
    nodePort: 30003
    targetPort: 80
  selector:
    tier: app